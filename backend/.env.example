# Database Configuration
MONGO_URL="mongodb://localhost:27017"
DB_NAME="virtualfit_database"

# CORS Configuration
CORS_ORIGINS="http://localhost:3000,*"

# Google Cloud Vertex AI Configuration
GOOGLE_CLOUD_PROJECT_ID="your-project-id"
VERTEX_AI_REGION="us-central1"
VERTEX_AI_ENDPOINT_ID="your-endpoint-id"
GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"

# FASHN API Configuration (Fallback)
FASHN_API_KEY="your-fashn-api-key"
FASHN_BASE_URL="https://api.fashn.ai/v1"

# Redis Configuration (for job storage)
REDIS_URL="redis://localhost:6379"

# Celery Configuration (for async processing)
CELERY_BROKER_URL="redis://localhost:6379"
CELERY_RESULT_BACKEND="redis://localhost:6379"

# File Storage Configuration
RESULT_STORAGE_URL="https://cdn.virtualfit.com"
RESULT_EXPIRY_DAYS=30

# API Configuration
API_RATE_LIMIT_PER_MINUTE=60
API_RATE_LIMIT_PER_HOUR=1000

# OpenAI Configuration (if still needed)
OPENAI_API_KEY="your-openai-api-key"