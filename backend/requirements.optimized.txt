# Ultra-optimized requirements for VirtualFit Backend - Core 3D Virtual Try-On
# Target: <1GB Docker image while preserving ALL 3D functionality

# Core FastAPI and server dependencies (minimal versions)
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-dotenv==1.0.1
pymongo==4.5.0
pydantic==2.6.4
email-validator==2.2.0
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
motor==3.3.1
python-jose[cryptography]==3.3.0
requests==2.31.0
python-multipart==0.0.9
aiofiles==24.1.0

# AI and image processing (essential only)
# openai==1.99.9  # Removed - moved to 3D hybrid approach
pillow==10.0.0
fal-client==0.7.0

# Essential 3D virtual try-on dependencies (minimal versions)
mediapipe==0.10.21
opencv-python-headless==4.8.1.78
trimesh==4.0.5
pybullet==3.2.7
scipy==1.11.4
numpy==1.26.4

# AI enhancement dependencies (CPU-optimized)
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu  
diffusers==0.30.0
transformers>=4.30.0
accelerate>=0.20.0

# Note: PyTorch and AI enhancement dependencies for CPU-only installation
