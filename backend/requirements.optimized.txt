# Production requirements for VirtualFit Backend - Core 3D Virtual Try-On
# Optimized for deployment size while preserving ALL functionality

# Core FastAPI and server dependencies
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
pymongo==4.5.0
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
passlib>=1.7.4
motor==3.3.1
python-jose>=3.3.0
requests>=2.31.0
python-multipart>=0.0.9
bcrypt>=4.0.1
aiofiles

# AI and image processing (required for virtual try-on)
openai>=1.0.0
pillow>=10.0.0
fal-client

# Essential 3D virtual try-on dependencies
mediapipe==0.10.21
opencv-python-headless==4.8.1.78
trimesh[easy]==4.0.5
pybullet==3.2.7
scipy==1.11.4
numpy>=1.21.0,<2.0.0

# AI enhancement with CPU-optimized PyTorch
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu
diffusers==0.30.0

# AI enhancement dependencies (required for Stable Diffusion)
transformers==4.35.2
accelerate==0.24.1

# Note: Removed unused dependencies to reduce image size:
# - Development tools: pytest, black, isort, flake8, mypy
# - Unused libraries: open3d, scikit-image, huggingface_hub
# - Unused utilities: jq, typer, pandas, boto3, requests_oauthlib, cryptography, tzdata
# - torchaudio (not needed for image processing)
